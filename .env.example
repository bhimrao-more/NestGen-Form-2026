# FlytBase Event Landing Page — Environment Variables
# Copy to .env.local if needed. Most event pages don't need env vars.

# Only needed if your form submits to an external API
# FORM_ENDPOINT=https://your-crm-or-api.com/submit
